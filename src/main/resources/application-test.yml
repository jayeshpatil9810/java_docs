spring:
  application:
    name: shutterstockApi-test


  datasource:
    url: jdbc:mysql://localhost:3306/shutterstock
    username: root
    password: Suvam@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        use_sql_comments: true
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

logging:
  file:
    name: ./logs/sstk-test.log