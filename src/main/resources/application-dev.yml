spring:
  application:
    name: shutterstockApi-dev-vadodara

  datasource:
    url: jdbc:mysql://192.168.8.64:3306/sstk-vadodara
    username: root
    password: Sstk@2024
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        use_sql_comments: true
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  file:
    name: ./logs/sstk-dev.log